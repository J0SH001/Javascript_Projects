function alex() { //Declaring the function
    var lex = 5 * (3 + 2); //Defining the variable
    document.getElementById("bert").innerHTML = lex; // PEMDAS example, linking to HTML element
}

function alexander() { //Declaring the function
    var lexi = 9 / 3;//Defining the variable
    document.getElementById("echbert").innerHTML = lexi; //Division example, linking to HTML element
}

function alexa() { //Declaring the function
    var lexicon = 8 % 3; //Defining the variable
    document.getElementById("albert").innerHTML = lexicon; //Remainder example, linking to HTML element
}

function alexandria() { //Declaring the function
    var lexluther = 9 / 3; //Defining the variable
    document.getElementById("bert2").innerHTML = -lexluther; //Division inverse example, linking to HTML element
}

function dope() { //Declaring the function
    var teddy = 5; //Defining the variable
    teddy++; //Same as add 1, increment
    document.getElementById("1").innerHTML = teddy; //Increment example, linking to HTML element
}

function dopey() { //Declaring the function
    var teddy = 5; //Defining the variable
    teddy--; //Same as subtract 1, decrement
    document.getElementById("2").innerHTML = teddy; //Decrement example, linking to HTML element
}

window.alert(Math.random()); //Setting up an alert pop up and displaying a random number between 0 & 1